"use client"

import type React from "react"

// SillyBackground component for the website's background
const SillyBackground: React.FC = () => {
  return <div className="fixed inset-0 overflow-hidden bg-[#a5c0d4] z-0" />
}

export default SillyBackground

